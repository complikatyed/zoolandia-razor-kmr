@{
    ViewBag.Title = "Employees";
}

<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>
<ul>
    @{
        for (int i = 0; i < ViewBag.Employees.Count; i++)
        {
            <li>@ViewBag.Employees[i].EmployeeName</li>
        }
    }
</ul>